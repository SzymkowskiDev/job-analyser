import json
import logging
import os

log_format = '%(levelname)s: %(message)s'
logging.basicConfig(level=logging.INFO, format=log_format)


def io_skills_to_titles(input_skills: list) -> list:
    pass